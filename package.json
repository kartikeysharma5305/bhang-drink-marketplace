{
  "name": "bhang-marketplace",
  "version": "1.0.0",
  "scripts": {
    "install-frontend": "cd frontend && npm install --include=dev",
    "install-backend": "cd backend && npm install",
    "build": "npm run install-frontend && npm run install-backend && cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "license": "MIT"
}
